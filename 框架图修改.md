# SLPA-SEI 框架图修改建议

## 1. 审查结论

当前图的主要问题不是版式，而是图示语义与最终算法不一致：

- STDA 被表现为“伪标签传播 + 特征聚类”，但当前 STDA 实际上由条件熵与边缘熵共同约束当前预测；
- LTDA 被表现为“历史原型与当前原型逐类匹配”，但当前 LTDA 实际上比较当前批次平均预测分布与历史预测分布参考；
- 堆叠式历史卡片容易被理解为 memory bank 或历史样本重放，而当前方法只保存一个紧凑的 $C$ 维历史参考 $\mathbf q_t$；
- 图中没有体现 LTDA 的时间顺序、停止梯度、Jensen--Shannon（JS）对齐和指数移动平均（EMA）参考更新。

如果保持当前画法，审稿人可能把 SLPA-SEI 理解为聚类式伪标签方法，而不是双时间尺度预测对齐方法。因此，应先修正算法表达，再调整视觉风格。

## 2. 必须修改的文字

| 当前文字或图形 | 问题 | 建议替换 |
|---|---|---|
| `Current-batch refinement` | 未突出决策对齐 | `Current-set decision alignment` |
| `Current batch` | 圆点无法说明是信号、特征还是预测 | `Current predictions` 或 `Current prediction set` |
| `Pseudo-label propagation` | 当前 STDA 不包含伪标签传播 | 删除，改为 `Per-signal decisiveness (CE)` |
| `Compact clusters` | CE−ME 不直接优化特征聚类或类内紧凑性 | 改为 `Refined current decisions`，或拆成两个并行作用 |
| `History-anchored update` | 容易被理解为历史样本直接参与模型更新 | `Current-to-history distribution alignment` |
| `History memory` | 暗示历史样本缓存或 memory bank | `Historical reference` |
| `Current prototypes` | $\overline{\mathbf p}_t$ 是平均预测分布，不是类别原型 | `Current aggregate distribution` |
| 底部手写体 `STDA`、`LTDA` | 与顶部标题重复且不符合 IEEE 图示风格 | 删除 |

以下内容必须从当前版本中移除：

- `Pseudo-label propagation`；
- `Compact clusters`；
- `Current prototypes`；
- 表示多批历史样本缓存的堆叠式 `History memory`；
- 没有算法依据的特征空间决策边界。

## 3. STDA 面板的修改方案

### 3.1 正确的算法含义

STDA 的目标为

$$
\mathcal L_{\mathrm{STDA}}^t
=
\mathcal H_{\mathrm{CE}}^t
-
\mathcal H_{\mathrm{ME}}^t.
$$

它不是“当前批次—伪标签传播—聚类”的顺序流程，而是使用同一组当前预测形成两个互补约束：

1. 条件熵促进每个接收信号产生更明确的发射机决策；
2. 边缘熵避免当前信号集合的预测过度集中于少数注册发射机；
3. 两个目标共同改善当前预测结构。

### 3.2 推荐构图

将 STDA 改成“一个输入、两个并行分支、一个联合结果”：

```text
Current predictions {p_i^t}
        ├── Conditional entropy ── Per-signal decisiveness
        └── Marginal entropy ───── Aggregate non-concentration
                              ↓
                 Refined current decisions
```

推荐图中文字：

- `Current predictions $\{\mathbf p_i^t\}_{i=1}^{N_t}$`；
- `Per-signal decisiveness (CE)`；
- `Aggregate non-concentration (ME)`；
- `Refined current decisions`；
- `$\mathcal L_{\mathrm{STDA}}^t=\mathcal H_{\mathrm{CE}}^t-\mathcal H_{\mathrm{ME}}^t$`。

图形上可以用若干小型概率条形图表示预测由模糊变得明确。边缘熵分支可以用批次平均分布表示“不过度集中”，但不要画成严格均匀分布。均匀分布只能作为软性的 anti-collapse 参考，不能被解释为真实的发射机活动先验。

## 4. LTDA 面板的修改方案

### 4.1 正确的算法含义

LTDA 的对齐目标为

$$
\mathcal L_{\mathrm{LTDA}}^t
=
D_{\mathrm{JS}}
\left(
\overline{\mathbf p}_t
\middle\|
\operatorname{sg}(\mathbf q_{t-1})
\right).
$$

其中，$\overline{\mathbf p}_t$ 是当前批次平均预测分布，$\mathbf q_{t-1}$ 是上一时刻保存的历史预测分布参考。当前分布保持可微，只停止历史参考的梯度。

完成当前对齐目标构造后，再更新历史参考：

$$
\mathbf q_t
=
\mu\mathbf q_{t-1}
+
(1-\mu)\operatorname{sg}(\overline{\mathbf p}_t).
$$

EMA 负责形成紧凑历史参考，JS divergence 才是实际的当前到历史对齐目标。

### 4.2 推荐构图

用两个概率向量或概率条形分布替代彩色原型圆点：

```text
Historical reference sg(q_{t-1}) ─┐
                                   ├── JS alignment ── LTDA objective
Current aggregate distribution p̄_t ─┘                       │
                                                             ↓
                                                   EMA update q_t
```

推荐图中文字：

- `Historical reference $\operatorname{sg}(\mathbf q_{t-1})$`；
- `Current aggregate distribution $\overline{\mathbf p}_t$`；
- `JS alignment`；
- `EMA reference update`；
- 时间轴 `$t-1\rightarrow t\rightarrow t+1$`。

应明确表现以下顺序：

1. 使用 $\mathbf q_{t-1}$ 与 $\overline{\mathbf p}_t$ 构造 JS 对齐损失；
2. 执行当前的单步模型更新；
3. 将分离梯度后的 $\overline{\mathbf p}_t$ 写入新的历史参考 $\mathbf q_t$；
4. 将 $(\theta_t,\mathbf q_t)$ 传递至下一适应步。

不要再使用逐类虚线连接多个圆点，因为这种画法暗示类别原型的一一匹配，而 LTDA 比较的是两个完整的 $C$ 维概率分布。

## 5. 完整框架应补充的联合更新

如果该图承担完整 SLPA-SEI 方法总览的作用，应在两个模块之后加入

$$
\mathcal L_{\mathrm{SLPA\text{-}SEI}}^t
=
\mathcal L_{\mathrm{STDA}}^t
+
\lambda_{\mathrm{mec}}\mathcal L_{\mathrm{LTDA}}^t,
$$

并连接到 `Single-step BN update`。这用于说明：

- STDA 与 LTDA 共同作用于同一个联合目标；
- 两个模块更新同一组允许适应的 BN 参数，而不是分别更新两个模型；
- 每个当前信号批次最多执行一次在线更新；
- $\mathbf q_t$ 是传递到下一步的历史参考，不是模型参数或原始样本缓存。

如果联合目标已经出现在完整框架图的其他位置，则两个局部面板中不必重复。

## 6. 建议采用的最终英文文字

### STDA

- `STDA`
- `Current-set decision alignment`
- `Current predictions`
- `Per-signal decisiveness (CE)`
- `Aggregate non-concentration (ME)`
- `Refined current decisions`

### LTDA

- `LTDA`
- `Current-to-history distribution alignment`
- `Historical reference`
- `Current aggregate distribution`
- `JS alignment`
- `EMA reference update`

技术面板中优先使用具体变量，而不是抽象的 `memory` 或 `prototypes`：

- `Current predictions $\{\mathbf p_i^t\}$`；
- `Historical reference $\operatorname{sg}(\mathbf q_{t-1})$`；
- `Current aggregate distribution $\overline{\mathbf p}_t$`。

## 7. 视觉设计建议

1. 删除手写字体，统一使用 Arial、Helvetica 或与论文一致的字体；
2. 使用纯白或极浅灰背景，弱化当前米黄色底色和装饰性橙色边框；
3. 使用蓝色表示当前信息，使用橙色或绿色表示历史参考；
4. 不要让同一种颜色同时承担“模块类别”和“发射机类别”两种含义；
5. 统一实线与虚线的语义。若虚线表示停止梯度，应在整张图中保持一致；
6. 删除特征聚类和决策边界图形，因为当前目标不直接优化这些几何量；
7. 在 LTDA 下方加入时间轴和 EMA 更新，减少无意义空白；
8. 增加顶部和右侧导出边距，避免箭头被裁切；
9. 按论文最终尺寸检查，缩放后的最小字号建议不低于 8--9 pt；
10. 若用少量彩色类别作示意，应加入 $c=1,\ldots,C$ 或省略号，避免被理解为固定四类任务。

## 8. 建议图注

> **Overview of the two complementary modules in SLPA-SEI. STDA coordinates per-signal decisiveness and aggregate non-concentration within the current signal set. LTDA aligns the differentiable current aggregate prediction distribution with a detached historical reference and subsequently updates the reference through exponential moving averaging.**

## 9. 修改完成后的检查清单

- [ ] 图中只有 STDA 和 LTDA 两个活动模块；
- [ ] STDA 明确对应 CE−ME，没有伪标签传播或特征聚类；
- [ ] LTDA 明确比较 $\overline{\mathbf p}_t$ 与 $\operatorname{sg}(\mathbf q_{t-1})$；
- [ ] 只有历史参考被停止梯度，当前分布保持可微；
- [ ] JS 对齐发生在 EMA 写入 $\mathbf q_t$ 之前；
- [ ] 图中没有 memory bank、历史原始信号重放或类别原型匹配暗示；
- [ ] 完整框架中体现联合目标和一次 BN-only 在线更新；
- [ ] 数学符号、正文、伪代码和框架图描述同一算法版本；
- [ ] 所有文字在论文最终缩放尺寸下仍可辨认。
